# onnxruntime-node ships prebuilt CPU binaries inside the npm package; this stops npm install
# from also downloading the multi-GB CUDA execution-provider bundle the CPU-only runtime never uses.
onnxruntime-node-install=skip
